combined assesment test of day 1,2,3 .
 
Q1: What is the key difference between const and let, and when should you choose one over the other?
 
Q2: What is the output of 5 == "5" vs. 5 === "5", and why do they differ?
 
Q3: Name the 6 falsy values in JavaScript. If a variable holds "0" (a string containing zero), is it truthy or falsy?
 
Q4: What will a function return if it does not have an explicit return statement?

Q5: What is "Block Scope", and what happens if you try to log a variable outside the {} where it was defined with let or const?
 
Q6: What will print to the console when running this code?
 
let total = 10;
const addFive = (num = 0) => {
  return num + 5;
};
 
console.log(addFive());
console.log(addFive(total));
console.log(total);
 
Q7: What will print to the console?
 
const userRole = "";
const fallbackRole = "guest";
 
const finalRole = userRole || fallbackRole;
console.log(finalRole);
 
Coding Challenges
 
Q8: Even or Odd Checker
 
Q9: Secure Login Checker
 
 
Q10: Discount Calculator with Boundary Checks

