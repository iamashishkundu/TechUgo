/*
 
Exercise 2: The String vs. Number Trap
 Create a variable valA containing the number 50.
 Create a variable valB containing the string "50" (with quotes).
 Log both variables using typeof to see their types in the terminal.
 Try adding them: console.log(valA + valB);. Notice what happens when you add a number to a string.
 
 */

var valA = 50;
var valB = "50";

console.log(typeof(valA));
console.log(typeof(valB));
console.log(valA+valB);